#include <stdio.h>

int main()
{
    int binary;
    printf("Insert binary number (0 and 1): ");
    scanf("%d", &binary);
    printf("You entered: %d\n", binary);
    return 0;

    char name[50];
    printf("Insert name");
    scanf("%s", &name);
    printf("You entered: %s\n", name);
}